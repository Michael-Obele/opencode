.git
!.opencode
!.opencode/skills-global
.sst
.turbo
.wrangler
node_modules
**/node_modules
**/.output
**/dist
**/.turbo
**/.vite
**/coverage

# Slim-fly: never shipped at runtime. These dominate the upload size — the test

# suites alone are ~18MB, artifacts/ is ~8MB.

artifacts
sdks
specs
perf
**/test
**/tests
**/e2e
**/_.test.ts
\*\*/_.spec.ts
**/**snapshots\*\*
\*\*/playwright.config.\*

# Fly slim: keep the embedded app dist, but don't upload local node_modules/dist

!packages/app/dist

# Never ship web (SvelteKit) or heavy dev artifacts on Fly — app is the embedded UI

packages/web
packages/desktop
packages/console
packages/storybook

# NOTE: tsconfig\*.json files must NOT be excluded — Bun resolves the `@/` path

# aliases from them at runtime.
