[build]
  command = "bun run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

# SvelteKit with adapter-netlify handles redirects via SvelteKit routing.
# Add custom headers/redirects below as needed, e.g.:
# [[redirects]]
#   from = "/api/*"
#   to = "/.netlify/functions/:splat"
#   status = 200
